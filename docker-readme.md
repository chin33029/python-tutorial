# Common commands
```bash
# startup mongo (You must be in this directory)
docker-compose up -d

# See what containers are running
docker ps

# See the logs of all the services defined in docker-compose.yml
docker-compose logs

# Shutdown all containers/services defined in the docker-compose.yml
docker-compose down


```